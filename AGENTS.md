# Garage Research 开发约定

- 不保留废弃界面、兼容层或回退架构。选择满足当前需求的最简完整实现，优先复用现有库，模块职责清晰。
- 市场看板是用户管理的自选列表：创建、改名、调整顺序、增删成员。Coverage 是独立的持续跟踪工作区；简单模型只在 Coverage 内生成和展示。
- **永远不要在用户界面暴露实现细节。**禁止 v1/v2/v4、报告版本、研究服务在线、本地调度、任务队列、数据库、模型快照、数据管线等内部术语。开发文档与诊断日志可以记录它们，用户页面不能。
- 用户只需要知道研究是否完成、是否更新、跟踪是否开启。不要展示下一次自动报告的具体时分、裸时间戳、“最新 v4 · 09/07 23:30”之类拼接文本。
- 市场价旁不展示 Finnhub/FMP/API 等供应商标识。必要的数据质量说明用概括性状态加 info icon，出处放到说明或报告附录中。
- 假设和模拟数据必须如实说明，但不能铺满重复提示。模拟数字不冒充真实行情或财报；不隐藏信息缺口。
- 不使用 WORKSPACE/MARKETS 等无业务价值的英文眉题。界面文案必须面向投资研究者，短、具体、完整。
- 一个操作组最多 5 个控件。手机按钮文字必须可读，文字和图标与按钮尺寸协调；扩大触摸命中范围不能把信息标识或小图标撑成大按钮。
- 增加信息密度依靠有用的数据、图表和研究摘要，不能靠堆说明文字、装饰性卡片或虚构精确数字。
- 下载的研报采用专业股票研究报告结构：摘要、业务与竞争、财务、估值、现金流、催化、风险、跟踪事项、数据来源。参考 FinRobot 原版等宽左右分栏、分析和图表并排的模板；禁止只有几段短文的简陋单栏排版。
- 报告必须有真正根据数据生成的图表，PDF 与 HTML 都可阅读；数据缺失时保留完整结构并明确显示缺失，不生成模拟图表，并在 DATA_REQUIREMENTS.md 记录缺口、所需 API/权限与解决办法。
- 修改后验证关键交互、手机布局、报告实际渲染和文件下载。不能只凭编译通过宣称完成。

操作文案在上下文已明确对象时只使用动词，如「阅读」「编辑」「添加」，不重复「阅读研报」。Info 提示图标使用共享 InfoHint：14px 细线图标、16px 可视区域、透明背景，不加灰色方块；仅键盘聚焦显示焦点环。卡片不重复放置无操作含义的装饰图标。图表坐标标签与图例必须分区留白，避免文字相撞。

加载规范：每个独立内容区域只允许一个加载中间态，统一使用共享 LoadingState / LoadingBoundary。关联请求并行启动，由父区域汇总 pending；禁止先显示父级加载，再出现子级加载。只显示 spinner，加载描述仅用于 aria-label，不得显示「加载中」「加载完整日线」等文字。悬停光标保持默认箭头。

项目统一 LoadingScope：页面、懒加载、弹窗及子模块向最外层作用域汇总加载请求，全应用只显示一个 spinner。已有数据的后台刷新不新增中间态。子级禁止自行绘制 spinner、文字或另一套 skeleton；统一调用 LoadingState，保留可访问名称。

情景、筛选和标签切换不得卸载现有表格、重置纵向/横向滚动位置或改变用户阅读位置。数据更新保留当前内容直至新结果就绪；仅显式导航到新页面时重置滚动。一般说明使用 * 脚注；引用使用 [1]、[2] 编号，左对齐置于内容下方，不与操作区混排。

假设与参数面板优先以只读表格展示，点击单项后才出现编辑控件；禁止将大量输入框同时铺开。AI推荐自动作为默认假设；只在编辑单项时显示输入控件。用户修改按字段持久保存，跟踪周期更新仅覆盖未手动设定的字段。首轮推荐使用统一spinner，刷新保留上次有效结果，禁止要求用户再点击采用AI建议。移动端标题和次要图标同一行，情景选择单独一行。引用按照正文首次出现顺序编号，脚注编号占固定列、正文统一左对齐。推导过程或实现说明不放在卡片副标题。

历史日程使用固定条数分页，不使用不断追加记录的“更多”；分页不累积卡片高度。

正式运行禁止模拟行情、财务、估值指标和演示研报回退。缺失事实为 null 或不可用状态。调试仅通过 devtools.fixture_app 显式注入适配器，在独立端口与 .desk-fixture 数据目录运行；正式模块不得导入 devtools。预测假设不等于历史事实，必须保留口径区分。

数据消费必须通过DataAccess领域入口和FinancialData指标模块；不在页面或卡片内解析供应商报表，不从供应商专用缓存读取财务基期。财务补值严格匹配期间和币种；派生指标保留输入出处。历史净利润、经营现金流取已披露值，不用预测模型公式改写历史事实。

## 后台任务反馈

- 短数据读取使用统一spinner；生成研报等持续后台任务使用 TaskProgress 阶段反馈，不挂起页面，也不冒充普通加载状态。
- 提交成功即返回任务标识并关闭提交弹窗，用户留在原页面；任务中心跨页面展示等待、执行、完成和失败，完成后提供结果入口。
- 进度来自后端已完成阶段，不模拟时间百分比。错误保留已完成阶段并提供重试；不要求用户重复提交等待中的任务。
- TaskProgress 为无业务状态的共享展示组件，任务获取、排队、重试和结果导航由宿主提供。
