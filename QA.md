# MVP 验收记录

日期：2026-09-07。本地运行，未进行云部署或付费升级。

## 已完成

- `pnpm build`：TypeScript 检查与 Vite 生产构建通过。页面按路由拆包。
- `pnpm format:check`、Ruff 检查：通过。
- `pytest tests/desk -q`：14 项通过。包含模型会计恒等式、情景、非法参数、任务并发去重、文件、快照、调度、暂停、重启恢复、接口验证、模型失败与手动/定时合并。依赖有 2 条测试客户端弃用提示，不影响结果。
- Infisical dev 环境：找到 15 项配置；Finnhub 报价和配置的 Qwen 模型实际返回 HTTP 200；FMP 当时返回 HTTP 429，因此财务/历史曲线明确使用模拟数据。可版本控制文件扫描 10 个注入凭据值，0 处匹配。
- 浏览器实际操作：搜索并添加 AVGO，打开详情，再移出自选；自选恢复为 8 项。
- 浏览器实际操作：NVDA Research 对话框填写重点，生成 v4；后台 2026-09-07 15:30:36 UTC 接收，15:30:56 UTC 完成。真实模型叙述，模拟财务输入明确标注。
- Coverage：AAPL 加入后自动生成 v1，暂停/恢复后生成 v2；NVDA 在手动研究过程中加入 Coverage，没有额外重复任务。
- 最终跟踪配置：NVDA 每日，AAPL 每周，均启用。下一次时间保存在数据库；本地服务运行时生效。
- 模型联动：浏览器将 NVDA 增速从 20% 改为 25%，2028E 收入变为 254,876.953125 百万美元；归档 v3 仍为 20%。随后已恢复 20%。
- 界面：查看浅色/深色外观、1440px 桌面布局、390px 手机 Coverage 与模型表；页面宽度 390px，无 body 横向溢出，模型表存在 20 行，横向滚动能到达 2028E。
- K 线切换：浏览器图表无障碍描述从模拟价格走势变为模拟 K 线走势。
- PDF：渲染并检查 NVDA 报告，修正 CID 字体中点缺字、标题孤行、长假设行及来源 URL 溢出。正文、模型、来源分页完整。
- 文件下载：PDF/HTML/Markdown/CSV/JSON 五个下载接口均 HTTP 200，返回字节与归档文件 SHA-256 完全相同。浏览器下载菜单已验收；自动化工具等待 Chrome 下载事件时超时，因此不将浏览器下载事件记录为通过，改用实际 HTTP 响应及文件一致性验证交付。
- 重启：报告和 Coverage 状态持久保留，健康检查显示调度 running。

## 研究产物

- NVDA v4：`1b4880c8c75d4788b109b35b989f2f7f`（从浏览器发起的最终版本）。
- NVDA v3：`ba56f63a8de249cba1e697d399f44f0c`。
- AAPL v1：`054d031a5d8f469c855256126138a3c2`（首次自动研究）。
- AAPL v2：`b8b6c1e528024eea905f9e58d829d5a7`（恢复跟踪后的自动研究）。
- NVDA 初期 v1/v2 超时记录保留在任务历史中。按 Model Studio 官方参数关闭 Qwen 默认思考后，完整报告已连续成功。

文件位于 `.desk/reports/<id>/report.{pdf,html,md,csv,json}`。数据与 QA 图片被 Git 忽略；业务源码、锁文件和验收文档可正常版本控制。

## 当前范围

单用户本地 MVP；不支持公网多用户鉴权、完整三表或自动交易。缺失数据不伪造为真实数据；模拟输入不构成真实投资评级。Tavily / Exa 尚未启用，研究新闻来自 Finnhub。无需充值即可使用演示研究。
