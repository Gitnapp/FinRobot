# 投研数据接入与缺口

主界面只显示研究者需要的状态。这里记录实现要求和数据缺口，不能把这些术语直接塞进产品页面。

| 能力 | 当前实现 | 真实数据所需条件 | 不可用时的交付 |
|---|---|---|---|
| 报价、市值 | Finnhub quote/profile2 | 已有 FINNHUB_API_KEY | 明确标记的示例报价 |
| PE、Beta、52 周区间、增长率 | Finnhub stock/metric | 免费额度与标的权限；接口逐项校验 | 按字段说明示例指标，不伪装成实时 |
| 历史日线、均线、波动和回撤 | FMP 日线；代码计算指标 | FMP historical-price-eod/full 权限；此前为 429 限流 | 完整示例走势、均线和区间图 |
| 收入、盈利、现金流预测 | FMP annual statements + 确定性计算 | income-statement/cash-flow-statement/balance-sheet-statement；币种、期末匹配 | 示例财务基期、预测表、盈利与现金流图 |
| DCF、估值敏感性 | 现金流、折现率、永续增长率、净债务、股数计算 | 经核对的真实财报、净债务、稀释股数；不得只用当前股价倒算 | 标注“假设测算”，展示情景估值与敏感性，不作真实评级 |
| 同业比较 | 同行业标的报价和基本财务 | Finnhub/FMP 的同口径最新值 | 同口径示例对比，独立说明其范围 |
| 财报与新闻催化 | Finnhub 公司新闻；按来源时间组织 | 更精确财报日历可增接 calendar/earnings；原文/SEC 公告用于核验 | 新闻清单和需核验事项，不编造公告日期 |
| 深度正文 | 已配置 Qwen，分章节研究 | 现有 OPENAI_API_KEY/BASE_URL/MODEL；长文分批并校验篇幅 | 可独立运行的完整演示报告，不省略图表和章节 |
| 广泛检索 | Tavily/Exa 密钥已发现 | 后续接入检索与原始财报全文，增加跨来源核验 | 本版基于可取得的公司资料、新闻、指标与模型，限制在附录披露 |

不自动购买套餐或充值。解除 FMP 配额/权限限制后切换真实输入即可复用现有图表和报告结构。

## 本轮核验（2026-09-08）

Finnhub 报价及已配置模型均返回 HTTP 200；FMP quote 和 income-statement 仍返回 HTTP 429。当前未充值，行情走势及年报不足部分以示例输入完整呈现。先恢复/等待免费额度再验证；如后续接口返回权限限制，再决定是否申请权限或替换数据源，不自动购买套餐。

研究正文已按来源编号校验并在错误时重写相关章节；这只能保证编号有效，不能代替原始财报与逐条事实核验。真实历史毛利率、ROIC、净债务、稀释股数等缺少依据时不补造。

## 新凭证接入结果

已改为读取项目根目录 .env（用户提供的凭证副本，Git 忽略、权限 600）。FMP quote、income-statement、cash-flow-statement、historical-price-eod/full 均返回 HTTP 200；Finnhub quote 和模型连接正常。应用层已验证 NVDA、AAPL 的报价、历史行情及财务基期均不再使用模拟数据。此前的 429 属于旧凭证检查结果，不代表当前状态。

凭证切换时清除了行情/财务缓存。历史研报及用户已有预测假设保持不变；如需让报告使用新财务基期，请重新生成研报。

## 催化日历与散户情绪（2026-09-08）

已实测当前凭证：Finnhub 财报日历与 Adanos Reddit 个股情绪均成功。标的详情与 Coverage 详情共用两个独立模块，前端不接触供应商密钥、URL 或原始响应。

- 催化日历当前覆盖有明确发布日期的财报（未来90天、过去30天）、时段、预期/实际EPS及预期收入。没有日期时显示空状态，不推算下一次财报日期。
- Retail Sentiment 当前覆盖 Reddit 最近7个UTC自然日的情绪分、热度、讨论量、看多/看空比例及每日讨论图。原始消息、X及其他平台尚未接入。不能把Reddit代表成整个散户市场。
- 后续催化类型：产品发布、监管审批、投资者日、股东大会、并购进度。需要公司IR公告/监管公告的结构化事件来源（含确认日期和原始链接），由同一适配器返回统一事件结构。当前不根据新闻标题猜日期或重要性星级。

接口：`GET /api/assets/{symbol}/signals/{catalysts|sentiment}`。统一返回 `status / as_of / data / reason`；各模块独立加载、不加入原详情的大请求。6小时持久缓存、相同请求合并、失败15分钟冷却；可用的旧数据最多保留7天且明确标记待更新。没有数据时返回null，绝不把缺失情绪当中性或0。演示模式不请求第三方且不编造日程。

`providers.py` 负责 FMP/Finnhub/Adanos 的认证、并发限制、超时、429冷却及无密钥错误；`market.py` 保留原有行情/财务适配；`signals.py` 负责事件/情绪的字段校验、归一化与缓存。替换供应商只改后端适配器。前端请求含30秒截止时间，新增模块支持取消与独立重试。免费Adanos额度有限，缓存策略按每标的每天最多4次读取设计；扩大Coverage数量前应根据实际月配额降低采集频率或升级。

官方字段依据：https://api.adanos.org/docs 、https://finnhub.io/docs/api/quote （Earnings Calendar）。测试覆盖并发去重、异常缓存、缓存过期、异常字段与模块隔离。

## 上市以来日线（2026-09-08）

`GET /api/assets/{symbol}/history` 通过 FMP profile 的 ipoDate 确定起点，按不超过1461天的时间段抓取 OHLCV，排序、去重、校验价格和日期。已落库缓存，自选8个标的均已实际拉取。AAPL 1980-12-12 起11525根、NVDA 1999-01-22 起6948根；当前最新交易日2026-09-04。AMD 上市日期1972-09-07，但供应商最早返回1980-03-17，11713根，`complete=false`，需要另有1972–1980历史覆盖的数据源补齐；不编造早期价格。

图表“全部”按需读取独立接口，提供缩放和K线；短周期行情与研究的一年风险指标保留原计算范围。历史窗口缓存7天、最新窗口6小时，历史价格重述可在刷新后反映。供应商错误不以模拟数据冒充全历史，任何可检测到的窗口缺口标为不完整。交易日内无报价的区间不能仅凭OHLC日线判断是否停牌。数据覆盖截至供应商可用的最新交易日。

日线一致性：所有交互走势图（市场预览、标的详情和Coverage）统一读取 `/api/assets/{symbol}/history`；1M/3M/1Y/全部只在同一份OHLCV序列上过滤日期，折线和K线使用同一close。禁止短周期改用另一来源、模拟数据或单独缓存。完整历史加载失败显示错误，不切换到另一套价格。

## 股票简单模型的规模—估值—价格推导

参考用户提供的《比特币价格与市值模型.xlsx》A7:G17 的“规模×单位价值→总估值÷数量→单位价格”关系，保留股票经营/现金流指标，新增净债务、隐含股权价值、摊薄股数、隐含每股价格，共24行。没有将数字货币用户数或占比强行映射为股票指标。

数据：净债务＝同一财年USD资产负债表的totalDebt−cashAndCashEquivalents；股数来自利润表weightedAverageShsOutDil（百万股），并非期末流通股数。预测净债务保持基期、预测股数按share_growth逐年变动。EV＝当年EBITDA×估值倍数；股权价值＝EV−净债务；价格＝股权价值÷股数。未来每股估值未折现，不是当前目标价。缺失净债务/股数或非正股数不生成价格；负股权价值不生成负股票价格。用户原有增长和利润率假设保持原值。

AAPL/NVDA已用真实报表验证三种情景。一般说明采用*，来源和框架引用采用[1]/[2]，导出CSV采用相同单位和引用约定。

## AI 假设建议与编辑交互

`POST /api/models/{symbol}/suggest` 使用当前配置的研究模型、真实财务基期和已有假设生成9项基准建议及对应理由。建议生成不写数据库；只有用户点击应用才调用原有保存接口。所有字段必须齐全并通过Assumptions上下限校验，输出失败允许一次有错误反馈的重写，总生成时限24秒。营运资金流量不解释为余额；缺乏证据不编造外部共识。面板默认只读表格，逐项编辑最多一个输入框；AI输出与当前值并列。

## TickFlow 中、美、港行情（2026-09-08）

已采用官方REST协议，通过现有异步HTTP层接入，无新增SDK依赖。未配置 TICKFLOW_API_KEY 时使用 https://free-api.tickflow.org/v1/ ，支持元数据与历史收盘日线；配置该key后请求正式服务和行情快照，需要相应订阅权限。免费服务不提供实时或分钟行情，不以免费收盘价冒充实时价。

已移除旧FMP价格/历史适配，交互图表、报价和一年技术指标统一来自TickFlow，使用比例前复权。单次10000根，超过时向前翻页；同日跨页复权数值已实测一致。按交易所时区将时间戳转换为日期，处理CNY/HKD/USD。AAPL为11525根（1980-12-12起），腾讯5476根（2004-06-16起），浦发6385根（1999-11-10起）。上市日期缺失时不宣称已覆盖上市首日，只展示供应商可用范围。成交量保持供应商原始口径。

搜索和添加支持名称或代码：NVDA/AAPL.US、600000.SH、000001.SZ、00700.HK。US后缀归一到现有美股代码，港股数字补齐5位，避免重复。覆盖清单现有43家公司已校验并接通行情，是否具备财务研究能力单独记录；A/H行情页面不展示美股模拟财报或自动启用美元估值。原财务/情绪/新闻来源不因接入行情而自动解锁。

官方依据：https://docs.tickflow.org/zh-Hans/sdk/python-quickstart ，https://docs.tickflow.org/zh-Hans/api-reference/openapi.json 。

## 覆盖行情与历史披露修复

覆盖行情独立于财务研报权限：`/api/coverage-market` 按全部已匹配证券读取统一快照，返回报价、日线摘要、区间统计及可用财务数据。没有交易代码的公司统一收纳，不再重复显示研究文案作为行情占位。已实测资产池45个证券均取得有效行情。

`/api/context/disclosures/{symbol}` 合并SEC submissions当前记录与历史分片，按原文URL去重。日历历史记录包含10-K、10-Q、8-K、20-F、6-K及修订文件，并支持年份筛选；披露日期不等于财报发布日，不把所有披露都解释成利好催化。AAPL368条（1994起），NVDA360条（1999起）。每次最多读取12个SEC归档分片，超过时通过complete=false明确表示覆盖未完整。
